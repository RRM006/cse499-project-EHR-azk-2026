# --- Secrets (NEVER commit) ---
.env
*.env
!*.env.example          # the example template IS committed (key names only)

# --- Python ---
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# --- Local data / DB (synthetic or consented samples only; not committed) ---
*.db
*.sqlite
*.sqlite3
data/
audio/

# --- Downloaded ML models (Phase 1+) ---
models/

# --- OS / editor noise ---
.DS_Store
Thumbs.db
.vscode/
.idea/
