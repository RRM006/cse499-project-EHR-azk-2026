# OPTIONAL — Qwen3-ASR-1.7B provider (local, CPU). Heaviest/riskiest engine.
#   pip install -r requirements-qwen.txt
#
# Kept in its own file so its dependencies never constrain the other engines.
# Pulls PyTorch (CPU) + ONNX Runtime. The 1.7B model auto-downloads from Hugging
# Face on first use (set QWEN_ASR_MODEL_DIR in backend/.env to use a local copy).
# NOTE: expect high latency on a 6-core CPU; verify Bangla quality before relying on it.

# torch left unpinned so pip selects a build matching your Python/OS.
qwen-asr
onnxruntime
torch
